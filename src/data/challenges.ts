import type { Challenge } from '../types';

export const challenges: Challenge[] = [
	// ═══════════════════════════════════════
	// FILE OPS (12 challenges)
	// ═══════════════════════════════════════
	{
		id: 'file-ops-01',
		category: 'file-ops',
		difficulty: 1,
		type: 'type-it',
		title: 'Find TypeScript Files',
		scenario: 'You just cloned a new project and want to see all the TypeScript files in the repo.',
		answer: ['find . -name "*.ts"', "find . -name '*.ts'"],
		hint: 'The find command takes a path, then -name with a glob pattern.',
		hint2: 'The syntax is `find <path> -name "<pattern>"`. Use `.` for the current directory and quote the glob.',
		explanation: '`find . -name "*.ts"` recursively searches from the current directory for files matching the glob *.ts. Quote the pattern so your shell doesn\'t expand it.',
	},
	{
		id: 'file-ops-02',
		category: 'file-ops',
		difficulty: 1,
		type: 'type-it',
		title: 'Nested Directories',
		scenario: 'Create the directory structure `src/components/ui` in one command, even if `src/` doesn\'t exist yet.',
		answer: ['mkdir -p src/components/ui'],
		hint: 'There\'s a flag that creates parent directories as needed.',
		hint2: 'The flag is `-p`. The full command starts with `mkdir -p` followed by the deepest path.',
		explanation: '`mkdir -p` creates the entire directory path, including any missing parents. Without -p, mkdir fails if the parent doesn\'t exist.',
	},
	{
		id: 'file-ops-03',
		category: 'file-ops',
		difficulty: 1,
		type: 'type-it',
		title: 'Recursive Copy',
		scenario: 'Copy the entire `templates/` directory into `backup/templates/`.',
		answer: ['cp -r templates/ backup/templates/', 'cp -R templates/ backup/templates/', 'cp -r templates backup/templates', 'cp -R templates backup/templates'],
		hint: 'You need a flag to copy directories recursively.',
		hint2: 'Use `cp -r source destination`. The `-r` (or `-R`) flag is what you need.',
		explanation: '`cp -r` (or -R) copies directories and their contents recursively. Without it, cp refuses to copy a directory.',
	},
	{
		id: 'file-ops-04',
		category: 'file-ops',
		difficulty: 2,
		type: 'predict',
		title: 'What Does -mtime -1 Match?',
		scenario: 'You run `find . -name "*.log" -mtime -1`. What does `-mtime -1` mean?',
		answer: ['modified less than 1 day ago', 'modified in the last day', 'modified in the last 24 hours', 'files modified within the last day'],
		hint: 'The minus sign before the number has a specific meaning in find.',
		hint2: 'The minus sign means "less than". So `-mtime -1` means modified less than 1 day ago — i.e., within the last 24 hours.',
		explanation: '`-mtime -1` means modified less than 1 day (24 hours) ago. The minus means "less than". `+1` means "more than 1 day ago". No sign means exactly 1 day.',
	},
	{
		id: 'file-ops-05',
		category: 'file-ops',
		difficulty: 2,
		type: 'type-it',
		title: 'Make It Executable',
		scenario: 'You wrote a deploy script `script.sh` but can\'t run it — permission denied. Give it standard executable permissions (rwxr-xr-x).',
		answer: ['chmod 755 script.sh', 'chmod +x script.sh'],
		hint: 'The octal number 755 means read/write/execute for owner, read/execute for group and others.',
		hint2: 'The octal digits map to rwx: 7=rwx, 5=r-x. `chmod` takes three digits: owner, group, others.',
		explanation: '`chmod 755` sets rwxr-xr-x. 7=rwx (owner), 5=r-x (group), 5=r-x (others). `chmod +x` is a shortcut to add execute for all.',
	},
	{
		id: 'file-ops-06',
		category: 'file-ops',
		difficulty: 2,
		type: 'type-it',
		title: 'Symbolic Link',
		scenario: 'Create a symlink called `current` that points to `releases/v2.1.0`.',
		answer: ['ln -s releases/v2.1.0 current'],
		hint: 'Symlinks use `ln` with the -s flag. Target comes first, then the link name.',
		hint2: 'The command pattern is `ln -s <target> <link_name>`. The target path comes before the link name.',
		explanation: '`ln -s target link_name` creates a symbolic link. The order matters: target first, then the name you want. Symlinks are like shortcuts — they point to the original.',
	},
	{
		id: 'file-ops-07',
		category: 'file-ops',
		difficulty: 2,
		type: 'fix-it',
		title: 'Fix the Tar Command',
		scenario: 'This command is supposed to create a .tar.gz archive of `dist/` but produces an error:',
		setup: '$ tar -cfz dist.tar.gz dist/\ntar: could not create archive',
		answer: ['tar -czf dist.tar.gz dist/', 'tar czf dist.tar.gz dist/', 'tar -czf dist.tar.gz dist', 'tar czf dist.tar.gz dist'],
		hint: 'c=create, z=gzip, f=filename. The archive name comes right after -f.',
		hint2: 'The `-f` flag must come right before the filename. Reorder the flags so `f` is last: `-czf`.',
		explanation: '`tar -czf archive.tar.gz dir/` creates (-c) a gzip-compressed (-z) archive. The -f flag must be followed immediately by the filename. To extract: `tar -xzf archive.tar.gz`.',
	},
	{
		id: 'file-ops-08',
		category: 'file-ops',
		difficulty: 3,
		type: 'type-it',
		title: 'Bulk Delete Temp Files',
		scenario: 'Delete every .tmp file in the project tree. Do it safely in one command with find.',
		answer: ['find . -name "*.tmp" -exec rm {} +', "find . -name '*.tmp' -exec rm {} +", 'find . -name "*.tmp" -delete', "find . -name '*.tmp' -delete"],
		hint: '`find` can execute a command on each match with -exec, or use -delete directly.',
		hint2: 'Use `find . -name "*.tmp"` with either `-exec rm {} +` or the shorthand `-delete` action.',
		explanation: '`find . -name "*.tmp" -exec rm {} +` passes all matches to rm in batches (+ is more efficient than \\;). The `-delete` flag is a built-in shortcut. Always test with `-print` first!',
	},
	{
		id: 'file-ops-09',
		category: 'file-ops',
		difficulty: 3,
		type: 'type-it',
		title: 'Top 5 Largest Dirs',
		scenario: 'You\'re running low on disk. Show the 5 largest immediate subdirectories, sorted biggest first, in human-readable sizes.',
		answer: ['du -sh */ | sort -rh | head -5', 'du -sh */ | sort -rh | head -n 5'],
		hint: '`du -sh */` sizes each subdir. Then sort in reverse human-numeric order and take the top 5.',
		hint2: 'Pipe three commands: `du -sh */` into `sort -rh` into `head -5`. The `-h` flag on sort handles human sizes.',
		explanation: '`du -sh */` shows disk usage for each subdirectory in human-readable form. `sort -rh` sorts reverse by human-numeric values (10G > 500M). `head -5` shows the top 5.',
	},
	{
		id: 'file-ops-10',
		category: 'file-ops',
		difficulty: 3,
		type: 'type-it',
		title: 'Purge Empty Dirs',
		scenario: 'After a cleanup, there are empty directories scattered everywhere. Remove all empty directories in one pass.',
		answer: ['find . -empty -type d -delete', 'find . -type d -empty -delete'],
		hint: '`find` has flags for empty items and for type (d = directory), plus a -delete action.',
		hint2: 'Combine three find predicates: `-empty`, `-type d`, and the `-delete` action.',
		explanation: '`find . -empty -type d -delete` finds directories that are empty and removes them. The order matters: -delete works depth-first, so nested empties are cleaned correctly.',
	},
	{
		id: 'file-ops-11',
		category: 'file-ops',
		difficulty: 2,
		type: 'fix-it',
		title: 'Fix the Find Command',
		scenario: 'This command is supposed to find all JavaScript files but it\'s broken:',
		setup: '$ find -name *.js',
		answer: ['find . -name "*.js"', "find . -name '*.js'"],
		hint: 'Two problems: missing starting path and unquoted glob (the shell expands *.js before find sees it).',
		hint2: 'Add `.` as the starting path and wrap `*.js` in quotes: `find . -name "*.js"`.',
		explanation: 'The fix needs a starting path (`.`) and quotes around the pattern. Without quotes, the shell expands `*.js` to matching files in the current directory, so find gets the wrong arguments.',
	},
	{
		id: 'file-ops-12',
		category: 'file-ops',
		difficulty: 1,
		type: 'predict',
		title: 'What Does stat Show?',
		scenario: 'You run `stat -f "%z" README.md` on macOS. What kind of value does it print?',
		setup: '$ stat -f "%z" README.md\n1247',
		answer: ['file size', 'the file size', 'file size in bytes', 'size in bytes', 'size'],
		hint: 'The %z format specifier in BSD stat relates to the file\'s data.',
		hint2: 'The `%z` format specifier in BSD stat outputs a number — it represents the file\'s size in bytes.',
		explanation: '`stat -f "%z"` on macOS (BSD stat) prints the file size in bytes. Note: GNU stat uses `stat -c "%s"` for the same thing — one of many BSD vs GNU differences.',
	},

	// ═══════════════════════════════════════
	// TEXT PROCESSING (12 challenges)
	// ═══════════════════════════════════════
	{
		id: 'text-01',
		category: 'text-processing',
		difficulty: 1,
		type: 'type-it',
		title: 'Hunt for TODOs',
		scenario: 'Search the entire project for TODO comments, recursively.',
		answer: ['grep -r "TODO" .', "grep -r 'TODO' .", 'grep -r TODO .', 'grep -rn "TODO" .', "grep -rn 'TODO' ."],
		hint: '`grep` has a recursive flag.',
		hint2: 'The recursive flag is `-r`. The full pattern is `grep -r <pattern> <path>`.',
		explanation: '`grep -r "TODO" .` searches all files under the current directory for lines containing "TODO". Add -n to include line numbers. For large repos, consider `rg` instead — it\'s much faster.',
	},
	{
		id: 'text-02',
		category: 'text-processing',
		difficulty: 1,
		type: 'type-it',
		title: 'Count the Lines',
		scenario: 'How many lines are in `file.txt`?',
		answer: ['wc -l file.txt', 'wc -l < file.txt', 'cat file.txt | wc -l'],
		hint: '`wc` counts words, lines, and characters. Pick the right flag.',
		hint2: 'The flag for lines is `-l`. The command is `wc -l <file>`.',
		explanation: '`wc -l` counts newline characters. It\'s one of the most common commands in pipeline work — you\'ll combine it with grep, find, and other tools constantly.',
	},
	{
		id: 'text-03',
		category: 'text-processing',
		difficulty: 1,
		type: 'fill-blank',
		title: 'Peek at a File',
		scenario: 'Fill in the blank to show the first 20 lines of a file:',
		setup: '___ -20 file.txt',
		answer: ['head'],
		hint: 'The opposite of `tail`.',
		hint2: 'The blank is a command that shows the beginning of a file. It takes a number flag like `-20`.',
		explanation: '`head -20 file.txt` prints the first 20 lines. Its companion `tail -20` prints the last 20. Both are essential for quickly inspecting large files and logs.',
	},
	{
		id: 'text-04',
		category: 'text-processing',
		difficulty: 2,
		type: 'type-it',
		title: 'Grep with Precision',
		scenario: 'Find all lines containing "function" in TypeScript files only, with line numbers.',
		answer: ['grep -rn "function" --include="*.ts" .', "grep -rn 'function' --include='*.ts' .", 'grep -rn function --include="*.ts" .', "grep -rn function --include='*.ts' ."],
		hint: '`--include` filters by filename pattern. -n adds line numbers.',
		hint2: 'Combine `-r` for recursive, `-n` for line numbers, and `--include="*.ts"` to restrict file types.',
		explanation: '`grep -rn "function" --include="*.ts" .` searches recursively (-r), shows line numbers (-n), and only looks in .ts files. The `--include` flag is critical for narrowing searches in large codebases.',
	},
	{
		id: 'text-05',
		category: 'text-processing',
		difficulty: 2,
		type: 'type-it',
		title: 'Find and Replace',
		scenario: 'Replace all occurrences of "localhost" with "0.0.0.0" in `config.txt`. Print the result (don\'t modify the file).',
		answer: ["sed 's/localhost/0.0.0.0/g' config.txt"],
		hint: 'The s command in sed does substitution. Add g for global (all occurrences per line).',
		hint2: 'The sed syntax is `sed \'s/old/new/g\' file`. The `g` flag makes it replace all occurrences, not just the first.',
		explanation: '`sed \'s/old/new/g\'` does a global substitution. On macOS, `sed -i \'\' \'s/old/new/g\' file` edits in place — note the empty string after -i, which BSD sed requires (GNU sed doesn\'t).',
	},
	{
		id: 'text-06',
		category: 'text-processing',
		difficulty: 2,
		type: 'type-it',
		title: 'Frequency Count',
		scenario: 'You have a file with one word per line. Show the most frequent words first, with counts.',
		setup: '$ cat words.txt\napple\nbanana\napple\ncherry\napple\nbanana',
		answer: ['sort words.txt | uniq -c | sort -rn', 'cat words.txt | sort | uniq -c | sort -rn'],
		hint: '`uniq -c` counts consecutive duplicates, so you need to sort first. Then sort the counts in reverse numeric order.',
		hint2: 'The pipeline is: `sort` first (to group duplicates), then `uniq -c` (to count), then `sort -rn` (to rank).',
		explanation: '`sort | uniq -c | sort -rn` is the classic frequency-counting pipeline. First sort groups identical lines together, then uniq -c counts them, then sort -rn orders by count descending.',
	},
	{
		id: 'text-07',
		category: 'text-processing',
		difficulty: 2,
		type: 'fill-blank',
		title: 'Extract First Column',
		scenario: 'Fill in the blank to print the first column from `access.log`:',
		setup: "awk '{print ___}' access.log",
		answer: ['$1'],
		hint: 'In awk, fields are numbered starting from 1, accessed with a special prefix.',
		hint2: 'Fields use a `$` prefix. The first field is `$1`, the second is `$2`, and `$0` is the whole line.',
		explanation: '`awk \'{print $1}\'` prints the first whitespace-delimited field of each line. $0 is the whole line, $1 is field 1, $2 is field 2, etc. awk is a small programming language hiding in your terminal.',
	},
	{
		id: 'text-08',
		category: 'text-processing',
		difficulty: 3,
		type: 'type-it',
		title: 'CSV Column Extraction',
		scenario: 'Extract the second column from a comma-separated CSV file `data.csv`.',
		answer: ["cut -d',' -f2 data.csv", 'cut -d"," -f2 data.csv', "cut -d ',' -f2 data.csv", 'cut -d, -f2 data.csv'],
		hint: 'The `cut` command has a delimiter flag (-d) and a field flag (-f).',
		hint2: 'The syntax is `cut -d\'<delimiter>\' -f<field_number> <file>`. Use comma as the delimiter and 2 as the field.',
		explanation: '`cut -d\',\' -f2` splits each line by comma and extracts field 2. For complex CSVs (quoted fields with commas), you\'ll want a proper CSV parser, but cut is perfect for simple data.',
	},
	{
		id: 'text-09',
		category: 'text-processing',
		difficulty: 3,
		type: 'predict',
		title: 'What Does tr Do Here?',
		scenario: 'What will this command output?',
		setup: "$ echo 'HELLO World' | tr '[:upper:]' '[:lower:]'",
		answer: ['hello world'],
		hint: '`tr` translates character-by-character based on the two sets.',
		hint2: '`tr` translates characters one-to-one between two sets. `[:upper:]` and `[:lower:]` are POSIX character classes.',
		explanation: '`tr \'[:upper:]\' \'[:lower:]\'` translates every uppercase character to its lowercase equivalent. `tr` reads from stdin only — it doesn\'t take a filename argument, so use redirection or a pipe.',
	},
	{
		id: 'text-10',
		category: 'text-processing',
		difficulty: 3,
		type: 'type-it',
		title: 'Sum a CSV Column',
		scenario: 'Sum all values in the 3rd column of a comma-separated file `sales.csv`.',
		answer: ["awk -F',' '{sum+=$3} END {print sum}' sales.csv", "awk -F, '{sum+=$3} END {print sum}' sales.csv"],
		hint: '`awk` can do math. Use -F to set the delimiter, accumulate in a variable, print in an END block.',
		hint2: 'Use `awk -F\',\' \'{sum+=$3} END {print sum}\' file`. The `-F` sets the delimiter, `$3` is the third column.',
		explanation: '`awk -F\',\' \'{sum+=$3} END {print sum}\'` sets comma as delimiter, adds each row\'s 3rd field to `sum`, then prints the total. This is faster than pulling in Python for quick calculations.',
	},
	{
		id: 'text-11',
		category: 'text-processing',
		difficulty: 2,
		type: 'fix-it',
		title: 'Fix the Sed Command',
		scenario: 'This command should replace "http" with "https" in-place on macOS, but it errors:',
		setup: "$ sed -i 's/http/https/g' urls.txt\nsed: 1: \"urls.txt\": invalid command code u",
		answer: ["sed -i '' 's/http/https/g' urls.txt"],
		hint: 'BSD sed (macOS) requires a backup extension after -i. Use an empty string for no backup.',
		hint2: 'On macOS (BSD sed), `-i` requires a backup suffix argument. Pass an empty string `\'\'` right after `-i`.',
		explanation: 'macOS uses BSD sed, which requires a backup suffix after -i. `sed -i \'\' \'s/...\'` uses an empty suffix (no backup). GNU sed on Linux doesn\'t need this. A common cross-platform gotcha!',
	},
	{
		id: 'text-12',
		category: 'text-processing',
		difficulty: 1,
		type: 'predict',
		title: 'What Does wc Print?',
		scenario: 'What will `wc -w` print for this input?',
		setup: '$ echo "hello world foo" | wc -w\n?',
		answer: ['3'],
		hint: '`-w` counts words, not lines or characters.',
		hint2: 'Count the whitespace-separated tokens in "hello world foo". Each space-separated word counts as one.',
		explanation: '`wc -w` counts whitespace-separated words. "hello world foo" has 3 words. Related: `-l` counts lines, `-c` counts bytes, `-m` counts characters.',
	},

	// ═══════════════════════════════════════
	// MODERN TOOLS (12 challenges)
	// ═══════════════════════════════════════
	{
		id: 'modern-01',
		category: 'modern-tools',
		difficulty: 1,
		type: 'type-it',
		title: 'Ripgrep Basics',
		scenario: 'Search the current directory for the string "TODO" using ripgrep.',
		answer: ['rg "TODO" .', "rg 'TODO' .", 'rg TODO .', 'rg TODO', 'rg "TODO"', "rg 'TODO'"],
		hint: 'ripgrep\'s command is `rg`. It searches recursively by default.',
		hint2: 'Just run `rg` followed by your search term. It searches recursively from the current directory by default.',
		explanation: '`rg "TODO"` searches recursively from the current directory. It\'s like `grep -rn` but 10-100x faster, respects .gitignore, and has better defaults. Install: `brew install ripgrep`.',
	},
	{
		id: 'modern-02',
		category: 'modern-tools',
		difficulty: 1,
		type: 'type-it',
		title: 'fd: Find Files Fast',
		scenario: 'Find all TypeScript files using fd.',
		answer: ['fd -e ts', "fd '.*\\.ts$'", 'fd -e ts .', "fd '\\.ts$'"],
		hint: '`fd` has an extension flag that\'s cleaner than glob patterns.',
		hint2: 'The `-e` flag filters by extension. The syntax is `fd -e <extension>`.',
		explanation: '`fd -e ts` finds all files with the .ts extension. fd is a modern alternative to find — simpler syntax, respects .gitignore, colorized output. Install: `brew install fd`.',
	},
	{
		id: 'modern-03',
		category: 'modern-tools',
		difficulty: 1,
		type: 'type-it',
		title: 'Pretty-Print JSON',
		scenario: 'You downloaded `config.json` from an API and it\'s a single ugly line. Pretty-print it.',
		answer: ['jq . config.json', "jq '.' config.json"],
		hint: 'jq with a simple identity filter formats the entire document.',
		hint2: 'The identity filter is just a dot: `jq .` Pretty-printing happens automatically.',
		explanation: '`jq \'.\'` is the identity filter — it outputs the input unchanged, but jq pretty-prints and colorizes by default. It\'s the single most useful jq command to know.',
	},
	{
		id: 'modern-04',
		category: 'modern-tools',
		difficulty: 2,
		type: 'fix-it',
		title: 'Fix the Ripgrep Search',
		scenario: 'This should search only TypeScript files for "import" but it errors:',
		setup: '$ rg --type ts "import"\nError: unrecognized flag',
		answer: ['rg -t ts "import"', "rg -t ts 'import'", 'rg -t ts import'],
		hint: '`rg` has a `-t` flag to filter by language/file type.',
		hint2: 'Use the short flag `-t` instead of `--type`. The syntax is `rg -t ts <pattern>`.',
		explanation: '`rg -t ts "import"` uses ripgrep\'s built-in type system. `-t ts` matches .ts and .tsx files. Run `rg --type-list` to see all supported types. Faster than `--include` patterns.',
	},
	{
		id: 'modern-05',
		category: 'modern-tools',
		difficulty: 2,
		type: 'type-it',
		title: 'Files Containing Pattern',
		scenario: 'List just the filenames (not the matches) that contain "TODO" using ripgrep.',
		answer: ['rg -l "TODO"', "rg -l 'TODO'", 'rg -l TODO'],
		hint: 'There\'s a flag that shows only the matching file names.',
		hint2: 'The flag is `-l` (lowercase L), which stands for "files with matches". It prints filenames only.',
		explanation: '`rg -l` (or `--files-with-matches`) lists only file names. It stops searching a file after the first match, making it faster. Counterpart: `-L` lists files without matches.',
	},
	{
		id: 'modern-06',
		category: 'modern-tools',
		difficulty: 2,
		type: 'fill-blank',
		title: 'fd + Execute',
		scenario: 'Fill in the flag to run wc on each .ts file found by fd:',
		setup: 'fd -e ts ___ wc -l',
		answer: ['-x', '--exec'],
		hint: 'fd has a flag to execute a command on each match.',
		hint2: 'The flag is `-x` (lowercase). It executes the given command on each match found by fd.',
		explanation: '`fd -e ts -x wc -l` runs `wc -l` on each .ts file found. `-x` executes the command for each result. Use `-X` (capital) to batch all results into one command call.',
	},
	{
		id: 'modern-07',
		category: 'modern-tools',
		difficulty: 2,
		type: 'type-it',
		title: 'jq: Extract Nested Data',
		scenario: 'Extract just the name of each user from this JSON file:',
		setup: '$ cat data.json\n{"users": [{"name": "Alice", "age": 30}, {"name": "Bob", "age": 25}]}',
		answer: ["jq '.users[] | .name' data.json", "jq '.users[].name' data.json"],
		hint: 'Use `.users[]` to iterate the array, then pipe to `.name`.',
		hint2: 'Use `.users[]` to iterate the array elements, then `.name` to extract the field. Chain with `|` or use `.users[].name`.',
		explanation: '`jq \'.users[] | .name\'` iterates over each element in the users array and extracts the name field. The `[]` operator unwraps an array, and `|` pipes each element to the next filter.',
	},
	{
		id: 'modern-08',
		category: 'modern-tools',
		difficulty: 3,
		type: 'fix-it',
		title: 'Exclude node_modules',
		scenario: 'This ripgrep search is too slow because it includes node_modules. Fix it:',
		setup: '$ rg "console.log"\n(thousands of results from node_modules...)',
		answer: ["rg 'console.log' -g '!node_modules'", 'rg "console.log" -g "!node_modules"', "rg 'console.log' -g '!node_modules/'", 'rg "console.log" -g "!node_modules/"', "rg console.log -g '!node_modules'"],
		hint: 'The `-g` flag accepts glob patterns. Prefix with `!` to negate.',
		hint2: 'Use the `-g` (glob) flag with a `!` prefix to exclude: `-g \'!node_modules\'`.',
		explanation: '`rg "pattern" -g \'!node_modules\'` excludes that directory. rg already respects .gitignore, but for untracked dirs this is useful. You can stack multiple -g flags.',
	},
	{
		id: 'modern-09',
		category: 'modern-tools',
		difficulty: 3,
		type: 'type-it',
		title: 'jq: Filter Objects',
		scenario: 'From a JSON array of objects, select only those where age is greater than 30.',
		setup: '$ cat people.json\n[{"name":"Alice","age":35},{"name":"Bob","age":25},{"name":"Carol","age":40}]',
		answer: ["jq '.[] | select(.age > 30)' people.json", "jq '[.[] | select(.age > 30)]' people.json"],
		hint: 'The `select()` function filters elements based on a condition.',
		hint2: 'Iterate with `.[]`, then pipe to `select(.age > 30)` to keep only matching objects.',
		explanation: '`jq \'.[] | select(.age > 30)\'` iterates the array and keeps only objects matching the condition. `select()` is jq\'s filter — it passes through items that match and drops the rest.',
	},
	{
		id: 'modern-10',
		category: 'modern-tools',
		difficulty: 3,
		type: 'type-it',
		title: 'Interactive History Search',
		scenario: 'Pipe your shell history through fzf to interactively search and select a previous command.',
		answer: ['history | fzf'],
		hint: 'fzf reads from stdin and provides an interactive fuzzy finder.',
		hint2: 'Just pipe `history` into `fzf`. No flags needed — fzf provides interactive filtering on any stdin.',
		explanation: '`history | fzf` pipes your command history into fzf\'s interactive fuzzy finder. You can type to search, then press Enter to select. fzf works with any line-based input — files, git branches, processes.',
	},
	{
		id: 'modern-11',
		category: 'modern-tools',
		difficulty: 1,
		type: 'predict',
		title: 'What Does bat Do?',
		scenario: 'You don\'t have `bat` installed. What does it replace, and how do you install it?',
		answer: ['cat', 'cat with syntax highlighting', 'a better cat'],
		hint: 'Think of a common command for viewing file contents.',
		hint2: 'It replaces `cat` — the command for viewing file contents. `bat` adds syntax highlighting and line numbers.',
		explanation: '`bat` is a `cat` replacement with syntax highlighting, line numbers, and git integration. Install it with `brew install bat`. It pages automatically for long files and supports hundreds of languages.',
	},
	{
		id: 'modern-12',
		category: 'modern-tools',
		difficulty: 2,
		type: 'predict',
		title: 'Meet eza',
		scenario: 'eza is a modern replacement for which classic command? Install it and try `eza --tree --level=2`.',
		answer: ['ls', 'ls command'],
		hint: 'It lists things. In color. With icons. And tree view.',
		hint2: 'It replaces the `ls` command. eza adds color, icons, git status, and tree views to directory listings.',
		explanation: '`eza` replaces `ls` with color, icons, git status, and tree views. Install: `brew install eza`. `eza --tree --level=2` shows a 2-level directory tree. Previously known as exa.',
	},

	// ═══════════════════════════════════════
	// PIPES & REDIRECTION (12 challenges)
	// ═══════════════════════════════════════
	{
		id: 'pipes-01',
		category: 'pipes',
		difficulty: 1,
		type: 'type-it',
		title: 'Count Files',
		scenario: 'Count how many files and directories are in the current directory.',
		answer: ['ls | wc -l', 'ls -1 | wc -l'],
		hint: 'Pipe the output of listing to a counting command.',
		hint2: 'Use the pipe `|` to connect `ls` output to `wc -l`. The pattern is `ls | wc -l`.',
		explanation: '`ls | wc -l` pipes the output of `ls` into `wc -l`, which counts lines. The pipe `|` connects stdout of the left command to stdin of the right. This is the essence of Unix philosophy.',
	},
	{
		id: 'pipes-02',
		category: 'pipes',
		difficulty: 1,
		type: 'type-it',
		title: 'Write to File',
		scenario: 'Write the text "hello world" to a new file called `greeting.txt`.',
		answer: ['echo "hello world" > greeting.txt', "echo 'hello world' > greeting.txt"],
		hint: 'The `>` operator redirects stdout to a file, creating or overwriting it.',
		hint2: 'The `>` operator redirects output to a file. The pattern is `echo "text" > filename`.',
		explanation: '`echo "hello world" > greeting.txt` redirects echo\'s output to a file. `>` creates the file if it doesn\'t exist, or overwrites it if it does. Use with caution — there\'s no undo.',
	},
	{
		id: 'pipes-03',
		category: 'pipes',
		difficulty: 1,
		type: 'type-it',
		title: 'Append to File',
		scenario: 'Add the line "new entry" to the end of `log.txt` without overwriting existing content.',
		answer: ['echo "new entry" >> log.txt', "echo 'new entry' >> log.txt"],
		hint: 'Double the redirect symbol to append instead of overwrite.',
		hint2: 'Use `>>` (double greater-than) instead of `>`. It appends rather than overwrites.',
		explanation: '`>>` appends to a file instead of overwriting. This is essential for logging and accumulating output. Compare: `>` destroys existing content, `>>` preserves it.',
	},
	{
		id: 'pipes-04',
		category: 'pipes',
		difficulty: 2,
		type: 'type-it',
		title: 'Log and See',
		scenario: 'Run `make build` and send both stdout and stderr to `build.log`, while still seeing the output on screen.',
		answer: ['make build 2>&1 | tee build.log'],
		hint: '`tee` writes to both stdout and a file. Merge stderr first with 2>&1.',
		hint2: 'The pattern is `command 2>&1 | tee file`. First merge stderr into stdout, then tee splits it.',
		explanation: '`2>&1` redirects stderr to stdout, merging them. `tee` then writes to both the terminal and the file. This pattern is essential for CI scripts where you need logs and live output.',
	},
	{
		id: 'pipes-05',
		category: 'pipes',
		difficulty: 2,
		type: 'type-it',
		title: 'Find and Grep',
		scenario: 'Find all .js files and search them for "console.log" using a pipe.',
		answer: ['find . -name "*.js" | xargs grep "console.log"', "find . -name '*.js' | xargs grep 'console.log'", 'find . -name "*.js" | xargs grep console.log'],
		hint: '`xargs` converts piped input into arguments for another command.',
		hint2: 'Pipe `find` results into `xargs grep`. The pattern is `find ... | xargs grep <pattern>`.',
		explanation: '`find | xargs grep` is a classic pattern. xargs takes the file list from find and passes them as arguments to grep. For filenames with spaces, use `find ... -print0 | xargs -0 grep ...`.',
	},
	{
		id: 'pipes-06',
		category: 'pipes',
		difficulty: 2,
		type: 'predict',
		title: 'Why Sort Before Uniq?',
		scenario: 'What happens if you run `uniq` without sorting first?',
		setup: '$ printf "apple\\nbanana\\napple" | uniq',
		answer: ['all three lines are printed', 'duplicates are not removed', 'it only removes adjacent duplicates'],
		hint: '`uniq` has a very specific limitation about which duplicates it catches.',
		hint2: '`uniq` only removes *adjacent* duplicates. Without sorting first, non-adjacent duplicates survive.',
		explanation: '`uniq` only removes *adjacent* duplicate lines. "apple" appears twice but not next to each other, so both survive. That is why `sort | uniq` is the standard pattern.',
	},
	{
		id: 'pipes-07',
		category: 'pipes',
		difficulty: 3,
		type: 'type-it',
		title: 'Process Substitution',
		scenario: 'Compare two sorted versions of different files without creating temp files. Diff file1.txt and file2.txt after sorting each.',
		answer: ['diff <(sort file1.txt) <(sort file2.txt)', 'diff <(sort file1) <(sort file2)'],
		hint: '`<(command)` creates a temporary file-like object from a command\'s output.',
		hint2: 'Wrap each sort in `<(...)`: `diff <(sort file1.txt) <(sort file2.txt)`. Each substitution acts like a temporary file.',
		explanation: '`<(...)` is process substitution — it runs a command and presents its output as a file. `diff <(sort file1) <(sort file2)` sorts both files on the fly and diffs them. Only works in bash/zsh.',
	},
	{
		id: 'pipes-08',
		category: 'pipes',
		difficulty: 3,
		type: 'fill-blank',
		title: 'Command Substitution',
		scenario: 'Fill in the blank to embed today\'s date in a filename:',
		setup: 'echo backup-___(date +%Y-%m-%d).tar.gz',
		answer: ['$('],
		hint: 'What syntax wraps a command so its output is substituted inline?',
		hint2: 'The syntax is `$( )` — a dollar sign followed by parentheses wrapping the command. The opening is `$(`.',
		explanation: '`$(date +%Y-%m-%d)` runs the date command and substitutes its output. `$()` is command substitution — one of the most powerful shell features. It works anywhere a string is expected.',
	},
	{
		id: 'pipes-09',
		category: 'pipes',
		difficulty: 3,
		type: 'fix-it',
		title: 'Silence the Errors',
		scenario: 'This find command works but spams "Permission denied" errors. Fix it:',
		setup: '$ find / -name "*.conf"\nfind: /private/var: Permission denied\nfind: /private/tmp: Permission denied\n/etc/nginx/nginx.conf',
		answer: ['find / -name "*.conf" 2>/dev/null', "find / -name '*.conf' 2>/dev/null"],
		hint: 'Stderr is file descriptor 2. Redirect it to the void.',
		hint2: 'Redirect file descriptor 2 (stderr) to `/dev/null`. Append `2>/dev/null` at the end of the command.',
		explanation: '`2>/dev/null` redirects stderr (fd 2) to /dev/null, discarding error messages. The three standard streams: 0=stdin, 1=stdout, 2=stderr. This is essential for noisy commands.',
	},
	{
		id: 'pipes-10',
		category: 'pipes',
		difficulty: 2,
		type: 'predict',
		title: 'Pipe Order Matters',
		scenario: 'What\'s the difference between these two commands?',
		setup: 'A: ls /fake 2>&1 | grep "No"\nB: ls /fake | grep "No" 2>&1',
		answer: ['A captures stderr in the pipe, B does not', 'A merges stderr before the pipe', 'A sends stderr through the pipe'],
		hint: 'Think about when the stderr redirect happens relative to the pipe.',
		hint2: 'In command A, `2>&1` happens before the pipe, so stderr flows through it. In B, only stdout enters the pipe.',
		explanation: 'In A, `2>&1` merges stderr into stdout BEFORE the pipe, so grep sees the error message. In B, only stdout goes through the pipe — stderr from ls still goes to the terminal. Redirect order matters!',
	},
	{
		id: 'pipes-11',
		category: 'pipes',
		difficulty: 1,
		type: 'fill-blank',
		title: 'Complete the Pipeline',
		scenario: 'Fill in the blank to count how many .ts files exist in the project:',
		setup: 'find . -name "*.ts" ___ wc -l',
		answer: ['|'],
		hint: 'What symbol connects the output of one command to the input of another?',
		hint2: 'The pipe symbol `|` connects stdout of one command to stdin of the next.',
		explanation: 'The pipe `|` connects stdout of the left command to stdin of the right. It\'s the fundamental building block of Unix command composition.',
	},
	{
		id: 'pipes-12',
		category: 'pipes',
		difficulty: 3,
		type: 'fill-blank',
		title: 'Tee Split',
		scenario: 'Fill in the blanks to send output to both a log file and the screen:',
		setup: 'npm test 2>&1 ___ tee test.log',
		answer: ['|'],
		hint: 'tee reads from stdin and writes to both stdout and a file.',
		hint2: 'The pipe symbol `|` feeds the merged output (stdout + stderr) into tee\'s stdin.',
		explanation: '`tee` splits a stream: one copy goes to the file, the other continues to stdout. Combined with `2>&1` to merge stderr, this captures everything while keeping output visible.',
	},

	// ═══════════════════════════════════════
	// GIT (11 challenges)
	// ═══════════════════════════════════════
	{
		id: 'git-01',
		category: 'git',
		difficulty: 1,
		type: 'type-it',
		title: 'Quick History',
		scenario: 'Show the last 10 commits in a compact one-line format.',
		answer: ['git log --oneline -10', 'git log --oneline -n 10', 'git log --oneline -n10'],
		hint: '`git log` has a format option and a count limiter.',
		hint2: 'Use `--oneline` for compact format and `-10` (or `-n 10`) to limit the count.',
		explanation: '`git log --oneline -10` shows each commit as a single line (short hash + message), limited to 10 entries. This is faster to scan than the full log format.',
	},
	{
		id: 'git-02',
		category: 'git',
		difficulty: 1,
		type: 'predict',
		title: 'Stash Pop',
		scenario: 'You ran `git stash` to save changes, then switched branches. What command restores your stashed changes?',
		answer: ['git stash pop', 'git stash apply'],
		hint: 'Think of the stash as a stack. What operation removes from the top?',
		hint2: 'The command is `git stash pop` to restore and remove, or `git stash apply` to restore and keep.',
		explanation: '`git stash pop` restores the most recent stash and removes it from the stash list. `git stash apply` restores but keeps it in the list. Use `git stash list` to see all stashes.',
	},
	{
		id: 'git-03',
		category: 'git',
		difficulty: 1,
		type: 'type-it',
		title: 'What\'s Staged?',
		scenario: 'See the diff of what you\'ve staged (added to the index) but haven\'t committed yet.',
		answer: ['git diff --staged', 'git diff --cached'],
		hint: 'Regular `git diff` shows unstaged changes. There\'s a flag for staged ones.',
		hint2: 'The flag is `--staged` (or its synonym `--cached`). Use it with `git diff`.',
		explanation: '`git diff --staged` (or `--cached`, same thing) shows what\'s in the staging area vs the last commit. Essential before committing to review exactly what you\'re about to commit.',
	},
	{
		id: 'git-04',
		category: 'git',
		difficulty: 2,
		type: 'type-it',
		title: 'Visual Branch History',
		scenario: 'Show a graph of all branches with one-line commit messages.',
		answer: ['git log --oneline --graph --all'],
		hint: 'Combine three flags: compact format, ASCII graph, and all branches.',
		hint2: 'Combine `--oneline`, `--graph`, and `--all` flags with `git log`.',
		explanation: '`git log --oneline --graph --all` draws an ASCII branch graph across all branches. This is the best way to understand branch topology without a GUI. Consider aliasing it to `git lg`.',
	},
	{
		id: 'git-05',
		category: 'git',
		difficulty: 2,
		type: 'type-it',
		title: 'Interactive Rebase',
		scenario: 'Squash, reword, or reorder the last 3 commits interactively.',
		answer: ['git rebase -i HEAD~3'],
		hint: 'Interactive rebase uses -i and a commit reference for how far back to go.',
		hint2: 'The syntax is `git rebase -i HEAD~N` where N is how many commits back to go.',
		explanation: '`git rebase -i HEAD~3` opens an editor showing the last 3 commits. You can reorder, squash, fixup, reword, or drop commits. The most powerful git command for cleaning up history before pushing.',
	},
	{
		id: 'git-06',
		category: 'git',
		difficulty: 2,
		type: 'fix-it',
		title: 'Fix the Cherry Pick',
		scenario: 'This was supposed to apply commit a1b2c3d but it merged the whole branch instead:',
		setup: '$ git merge a1b2c3d\n(merged entire branch history)',
		answer: ['git cherry-pick a1b2c3d'],
		hint: 'There\'s a command specifically for applying a single commit from elsewhere.',
		hint2: 'The command is `git cherry-pick` followed by the commit hash. It applies just that one commit.',
		explanation: '`git cherry-pick <hash>` applies a specific commit onto your current branch. Useful for backporting fixes. It creates a new commit with the same changes but a different hash.',
	},
	{
		id: 'git-07',
		category: 'git',
		difficulty: 2,
		type: 'type-it',
		title: 'Who Wrote This?',
		scenario: 'See who last modified each line of `server.ts`, with commit info.',
		answer: ['git blame server.ts'],
		hint: 'There\'s a git command named after what you do when you find a bug.',
		hint2: 'The command is named after assigning fault. It takes a filename as an argument.',
		explanation: '`git blame` annotates each line with the commit hash, author, and date of last modification. Despite the name, it\'s a debugging tool — use it to find when and why a line changed.',
	},
	{
		id: 'git-08',
		category: 'git',
		difficulty: 3,
		type: 'type-it',
		title: 'Recover Lost Commits',
		scenario: 'You did a hard reset and lost commits. Show the log of all reference changes to find and recover them.',
		answer: ['git reflog'],
		hint: 'Git never truly deletes commits. There\'s a log of where HEAD has been.',
		hint2: 'The command is `git reflog`. It shows where HEAD has pointed, even after destructive operations.',
		explanation: '`git reflog` shows everywhere HEAD has pointed, including after reset/rebase/checkout. Find the lost commit hash, then `git checkout <hash>` or `git reset --hard <hash>` to recover. Your safety net.',
	},
	{
		id: 'git-09',
		category: 'git',
		difficulty: 3,
		type: 'type-it',
		title: 'Binary Search for Bugs',
		scenario: 'Start a git bisect to find which commit introduced a bug. You know the current commit is bad and the commit `v1.0` was good.',
		answer: ['git bisect start HEAD v1.0', 'git bisect start && git bisect bad && git bisect good v1.0'],
		hint: '`git bisect` does a binary search through commits. You specify a bad and good reference.',
		hint2: 'The syntax is `git bisect start <bad_ref> <good_ref>`. HEAD is the bad commit, v1.0 is the good one.',
		explanation: '`git bisect start HEAD v1.0` begins a binary search: HEAD is bad, v1.0 is good. Git checks out the middle commit; you test and mark it `git bisect good` or `git bisect bad`. Finish with `git bisect reset`.',
	},
	{
		id: 'git-10',
		category: 'git',
		difficulty: 3,
		type: 'type-it',
		title: 'Author History',
		scenario: 'Show commits by "Alice" from the last 2 weeks in one-line format.',
		answer: ['git log --author="Alice" --since="2 weeks ago" --oneline', "git log --author='Alice' --since='2 weeks ago' --oneline"],
		hint: 'git log has --author and --since filters.',
		hint2: 'Use `--author="Alice"` and `--since="2 weeks ago"` as filters on `git log --oneline`.',
		explanation: '`git log --author="Alice" --since="2 weeks ago"` filters by author name (regex) and date. Combine with --oneline for compact output. Great for standup reports and code review.',
	},
	{
		id: 'git-11',
		category: 'git',
		difficulty: 1,
		type: 'fix-it',
		title: 'Fix the Git Command',
		scenario: 'This command should show staged changes but shows nothing even though files are staged:',
		setup: '$ git add .\n$ git diff\n(no output)',
		answer: ['git diff --staged', 'git diff --cached'],
		hint: '`git diff` without flags shows unstaged changes. What flag shows staged ones?',
		hint2: 'After `git add`, changes move to the staging area. `git diff` only shows unstaged changes — add `--staged`.',
		explanation: '`git diff` compares working directory to index (unstaged changes). After `git add`, changes are in the index — you need `git diff --staged` to see them. A very common gotcha for beginners.',
	},

	// ═══════════════════════════════════════
	// SYSTEM & PROCESS (11 challenges)
	// ═══════════════════════════════════════
	{
		id: 'system-01',
		category: 'system',
		difficulty: 1,
		type: 'type-it',
		title: 'Where Is It?',
		scenario: 'Find the full path to the `node` binary on your system.',
		answer: ['which node'],
		hint: 'There\'s a command that shows where an executable lives.',
		hint2: 'The command is `which` followed by the program name. It searches your PATH.',
		explanation: '`which node` prints the path of the executable that would run (e.g., `/opt/homebrew/bin/node`). Useful for debugging PATH issues when the wrong version runs.',
	},
	{
		id: 'system-02',
		category: 'system',
		difficulty: 1,
		type: 'type-it',
		title: 'Show Your PATH',
		scenario: 'Print the value of the PATH environment variable.',
		answer: ['echo $PATH'],
		hint: 'Environment variables are accessed with $ and printed with echo.',
		hint2: 'Use `echo` followed by `$PATH`. The dollar sign dereferences the variable.',
		explanation: '`echo $PATH` shows the directories your shell searches for commands, separated by colons. Order matters — the first match wins. This is the #1 thing to check when a command isn\'t found.',
	},
	{
		id: 'system-03',
		category: 'system',
		difficulty: 1,
		type: 'fill-blank',
		title: 'Find Node Processes',
		scenario: 'Fill in the blank to filter running processes for "node":',
		setup: 'ps aux | ___ node',
		answer: ['grep'],
		hint: 'What command searches text for a pattern?',
		hint2: 'The blank is the standard text-search command that filters lines matching a pattern.',
		explanation: '`ps aux | grep node` shows all processes matching "node". `a` = all users, `u` = user-oriented format, `x` = include background processes. Note: grep itself may appear in the results.',
	},
	{
		id: 'system-04',
		category: 'system',
		difficulty: 2,
		type: 'predict',
		title: 'Kill Signals',
		scenario: 'What\'s the key difference between `kill 1234` and `kill -9 1234`?',
		answer: [
			'kill sends SIGTERM, kill -9 sends SIGKILL',
			'SIGTERM vs SIGKILL',
			'SIGTERM can be caught, SIGKILL cannot',
			'kill -9 forces termination',
			'-9 cannot be caught or ignored',
		],
		hint: 'One can be caught by the process, the other can\'t.',
		hint2: 'Default `kill` sends SIGTERM (signal 15), which the process can catch and handle gracefully. `-9` sends SIGKILL, which cannot be caught.',
		explanation: '`kill` sends SIGTERM (15) — the process can catch it and shut down gracefully. `kill -9` sends SIGKILL — the kernel terminates it immediately, no cleanup. Always try SIGTERM first.',
	},
	{
		id: 'system-05',
		category: 'system',
		difficulty: 2,
		type: 'type-it',
		title: 'Port Detective',
		scenario: 'Your dev server won\'t start because port 3000 is already taken. Find out what\'s using it.',
		answer: ['lsof -i :3000'],
		hint: '`lsof` lists open files — and in Unix, network connections are files too.',
		hint2: 'The command is `lsof -i :<port>`. It lists processes with open file descriptors on that port.',
		explanation: '`lsof -i :3000` shows processes listening on port 3000. `lsof` means "list open files" — sockets are files in Unix. The output shows PID, user, and command. Then `kill <pid>` to free the port.',
	},
	{
		id: 'system-06',
		category: 'system',
		difficulty: 2,
		type: 'predict',
		title: 'Alias Persistence',
		scenario: 'You run `alias ll=\'ls -la\'` in your terminal. What happens when you open a new terminal window?',
		answer: ['the alias is gone', 'alias is lost', 'll is not defined', 'it does not persist'],
		hint: 'Think about what survives a shell session and what does not.',
		hint2: 'Aliases set in a session are not saved anywhere. They vanish when the shell exits.',
		explanation: 'Aliases defined in a terminal session are lost when it closes. To make them permanent, add them to `~/.zshrc` (macOS) or `~/.bashrc`. Then run `source ~/.zshrc` to apply.',
	},
	{
		id: 'system-07',
		category: 'system',
		difficulty: 2,
		type: 'type-it',
		title: 'Set an Env Variable',
		scenario: 'Set the environment variable `API_KEY` to `secret123` so child processes can see it.',
		answer: ['export API_KEY=secret123', "export API_KEY='secret123'", 'export API_KEY="secret123"'],
		hint: '`export` makes a variable available to child processes.',
		hint2: 'Use `export` before the assignment: `export VAR=value`. Without export, it\'s shell-local only.',
		explanation: '`export` makes the variable part of the environment, visible to any process launched from this shell. Without export, the variable is local to the shell. Use `env` or `printenv` to see all exports.',
	},
	{
		id: 'system-08',
		category: 'system',
		difficulty: 3,
		type: 'type-it',
		title: 'Background & Detach',
		scenario: 'Start a long-running `./build.sh` that survives after you close the terminal.',
		answer: ['nohup ./build.sh &', 'nohup ./build.sh > /dev/null 2>&1 &'],
		hint: '`nohup` prevents hangup signals. `&` runs in background.',
		hint2: '`nohup` prevents the hangup signal from killing the process. `&` puts it in the background. Combine both.',
		explanation: '`nohup command &` runs the command in the background and ignores the SIGHUP signal sent when the terminal closes. Output goes to nohup.out by default. For modern use, consider `tmux` or `screen`.',
	},
	{
		id: 'system-09',
		category: 'system',
		difficulty: 3,
		type: 'predict',
		title: 'df vs du',
		scenario: 'What is the key difference between `df -h` and `du -sh`?',
		answer: ['df shows filesystem space, du shows directory size', 'df shows disk free space, du shows disk usage of files', 'df is filesystem level, du is file level'],
		hint: 'One looks at the filesystem, the other looks at files and directories.',
		hint2: '`df` reports free space on mounted filesystems. `du` measures actual disk usage of specific files/directories.',
		explanation: '`df` (disk free) shows total/used/available space per mounted filesystem. `du` (disk usage) shows how much space specific files and directories consume. Use `df` for "am I running out of space?" and `du` for "what\'s using it?"',
	},
	{
		id: 'system-10',
		category: 'system',
		difficulty: 3,
		type: 'type-it',
		title: 'Filter Environment',
		scenario: 'Show all environment variables that contain "PATH" (case-insensitive).',
		answer: ['env | grep -i path', 'env | grep -i PATH', 'printenv | grep -i path', 'printenv | grep -i PATH'],
		hint: 'List all env vars, then filter with case-insensitive grep.',
		hint2: 'Use `env` (or `printenv`) piped to `grep -i`. The `-i` flag makes the search case-insensitive.',
		explanation: '`env | grep -i path` lists all environment variables and filters for "PATH" (case-insensitive). You\'ll see PATH, MANPATH, INFOPATH, and others. Useful for debugging environment issues.',
	},
	{
		id: 'system-11',
		category: 'system',
		difficulty: 2,
		type: 'fix-it',
		title: 'Fix the Export',
		scenario: 'This command sets a variable, but child processes can\'t see it:',
		setup: '$ MY_VAR=hello\n$ node -e "console.log(process.env.MY_VAR)"\nundefined',
		answer: ['export MY_VAR=hello', "export MY_VAR='hello'", 'export MY_VAR="hello"'],
		hint: 'Without a certain keyword, the variable stays local to the shell.',
		hint2: 'Add the `export` keyword before the variable assignment to make it visible to child processes.',
		explanation: 'Without `export`, the variable only exists in the current shell — child processes don\'t inherit it. `export MY_VAR=hello` puts it in the environment so `node`, `python`, etc. can access it.',
	},
];
